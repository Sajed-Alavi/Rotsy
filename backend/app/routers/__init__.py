"""API routers.

Each feature gets its own router module. Feature A (``storage``) is fully
implemented; Features B–H are scaffolded with stub routes returning HTTP 501
so the architecture is wired and discoverable.
"""
