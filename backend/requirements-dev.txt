# Test-only dependencies. Install alongside requirements.txt:
#   pip install -r requirements.txt -r requirements-dev.txt
# pytest/pytest-asyncio/pytest-cov are in requirements.txt itself now (also
# needed at runtime — see modules/sonar/coverage.py), so only truly dev/CI-only
# dependencies remain here.
-r requirements.txt
aiosqlite==0.22.1

# Dependency CVE audit. Run after any change to requirements.txt:
#   pip-audit -r requirements.txt
pip-audit==2.10.1
