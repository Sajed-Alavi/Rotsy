# ساخت Rotsy: یک کنسول امنیتی برای Nexus Repository Manager

**نسخه:** v1.0.0 · **تاریخ:** 2026-08-04

---

Sonatype Nexus Repository Manager جایی است که ایمیج‌های داکر بسیاری از تیم‌ها در آن نگهداری می‌شود، اما خود Nexus تقریباً هیچ اطلاعاتی درباره‌ی محتوای واقعی این ایمیج‌ها نمی‌دهد. آیا `myapp:latest` روی یک لایه‌ی پایه با شش آسیب‌پذیری CRITICAL وصله‌نشده اجرا می‌شود؟ Nexus چیزی نمی‌گوید. ما **Rotsy** را ساختیم تا به این سؤال پاسخ دهد — یک کنسول مبتنی بر FastAPI و React که جلوی Nexus می‌نشیند، هر چیزی که در آن ذخیره شده را با Trivy و Grype اسکن می‌کند، و «کلیک کردن روی Nexus و امیدوار بودن» را به یک فرآیند امنیتی واقعی تبدیل می‌کند: مخزن → ایمیج → تگ → گزارش آسیب‌پذیری، همراه با پشتیبان‌گیری زمان‌بندی‌شده، دسترسی مبتنی بر RBAC، و خروجی PDF برای هرکسی که نیاز دارد یک گزارش را به ممیز (auditor) تحویل دهد.

این نوشته بیشتر از اینکه «فهرست امکانات ما» باشد، روایت «چه چیزی واقعاً در حین ساخت این پروژه خراب شد و چطور آن را درست کردیم» است — چون بخش‌های جالب یک پروژه مثل این، تقریباً هیچ‌وقت مسیر بی‌دردسر (happy path) نیستند.

## دفتر ثبت اسکن رویدادمحور (event-driven)

اولین سؤال طراحی این بود: *چه زمانی* یک ایمیج اسکن می‌شود؟ پاسخ ساده‌لوحانه — «همه‌چیز را با یک تایمر اسکن کن» — خیلی زود از هم می‌پاشد. اسکن مجدد هر ایمیج در هر مخزن هر شب یعنی صف اسکن هم‌زمان با رشد رجیستری شما بدون محدودیت رشد می‌کند، و یعنی پاسخ به «آیا این ایمیج اخیراً اسکن شده؟» را یک کش با TTL می‌دهد، نه یک واقعیتِ قطعی.

Rotsy دقیقاً به دو دلیل اسکن انجام می‌دهد: یک ایمیج push شده (از طریق وب‌هوک Nexus، با یک مکانیزم polling جایگزین برای سامانه‌هایی که نمی‌توانند وب‌هوک راه‌اندازی کنند)، یا یک اپراتور درخواست کرده است. یک دفتر ثبت پایدار در Postgres (`ScannedImage`) هر ایمیجی را که Rotsy تا به حال دیده و وضعیت آن را ثبت می‌کند — `baseline`، `queued`، `scanned`، `failed` — بنابراین هیچ‌چیز به‌خاطر پاک شدن یک کش یا ری‌استارت یک پروسه، بی‌سروصدا دوباره اسکن نمی‌شود. اولین باری که یک مخزن مشاهده می‌شود، محتوای *موجودِ* آن به‌عنوان تاریخچه‌ی baseline ثبت شده و عمداً اسکن نمی‌شود — اضافه کردن یک مخزن با ۵۰۰ تگ موجود، به معنای ۵۰۰ اسکن در روز اول نیست.

## باگی که به ما یاد داد لغو کردن (cancellation) رایگان نیست

یک endpoint به شکل `POST /jobs/{id}/cancel` قبل از اینکه به آن دست بزنیم، از قبل وجود داشت. این endpoint فقط یک فیلد status در Redis را به `"cancelled"` تغییر می‌داد و کار را تمام‌شده تلقی می‌کرد. حتی docstring هم نوشته بود «لغو مشارکتی (cooperative) — کارگر (worker) وضعیت job را بین مراحل بررسی می‌کند.» اما هیچ‌چیزی این کار را نمی‌کرد. زیرپردازه‌ای (subprocess) که دانلود چند صد مگابایتی یک دیتابیس را انجام می‌داد، به‌صورت یتیم (orphaned) در پس‌زمینه به کار خود ادامه می‌داد، درحالی‌که رابط کاربری با خوشحالی گزارش می‌داد job لغو شده است.

راه‌حل این بود که دیگر وانمود نکنیم لغو کردن مشارکتی است و آن را واقعی کنیم: اکنون job runner همان `asyncio.Task` پشت هر job در حال اجرا را ردیابی می‌کند، و `cancel()` مستقیماً `.cancel()` را روی آن صدا می‌زند. اما این تمام ماجرا نیست — لغو کردن یک `asyncio.Task` که منتظر یک subprocess است، آن subprocess را نمی‌کشد. ما مجبور شدیم این منطق را در هر لایه‌ای که دستوری را اجرا می‌کند (حلقه‌های دانلود Trivy، Grype و `oras`) پیاده‌سازی کنیم: گرفتن `asyncio.CancelledError`، کشتنِ صریح پردازه‌ی فرزند، و *سپس* دوباره raise کردن خطا. اگر این مرحله را حذف کنید، فقط باگ را سخت‌تر برای دیدن کرده‌اید — job از رابط کاربری ناپدید می‌شود درحالی‌که دانلود همچنان پهنای باند مصرف می‌کند.

درس گرفته‌شده: «لغو» به‌عنوان یک قابلیت رابط کاربری، کار ساده‌ای است. لغو به‌عنوان یک تضمین واقعی — این پردازه همین حالا متوقف می‌شود و خودش دوباره شروع نمی‌شود — به سیم‌کشی واقعی در هر لایه‌ای نیاز دارد که می‌تواند از عمر درخواستی که آن را شروع کرده، بیشتر دوام بیاورد.

## کار کارآگاهی: چرا نوار پیشرفت دروغ می‌گوید؟

یک کاربر گزارش داد که رابط کاربری دانلود دیتابیس، عدد «~۱۱۹ مگابایت کل» را نشان می‌دهد، درحالی‌که انتقال واقعی همچنان از ۲۵۰ مگابایت هم بالاتر می‌رفت. مشکل در محاسبات نوار پیشرفت نبود — مشکل در *ورودیِ* آن بود. اندازه‌ی موردانتظار دانلود یک مقدار ثابت hardcode شده بود (`TRIVY_DB_MB = 50`) که یک‌بار نوشته شده و دیگر هیچ‌وقت بازبینی نشده بود، درحالی‌که دیتابیس واقعی منتشرشده به‌آرامی فراتر از آن رشد کرده بود. راه‌حل: تعیین اندازه‌ی *واقعی* از manifest تصویر OCI پیش از دانلود — همان تکنیکی که از قبل برای اسکنر دیگر استفاده می‌شد — و بازگشت به حدس hardcode شده (که به‌وضوح در رابط کاربری با برچسب «تخمینی» مشخص شده) تنها زمانی که آن جست‌وجو شکست بخورد.

همین غریزه‌ی دیباگ دوباره در یک دور رفع آسیب‌پذیری CVE نتیجه داد. یک اسکن Grype از ایمیج فرانت‌اند ما، شش آسیب‌پذیری CRITICAL در nginx گزارش داد که وصله‌های منتشرشده‌ای در نسخه‌های بسته‌ای مثل `1.28.3-r6` داشتند. `apk upgrade` این وصله‌ها را دریافت نمی‌کرد. دلیلش این بود: ایمیج پایه‌ی ما منابع بسته‌ی خودش را به کانال apk رسمی nginx.org محدود کرده بود، و *آن* کانال، بسته‌های امنیتیِ backport شده‌ی خود Alpine را حمل نمی‌کند — وصله‌ی `-r6` در مخزن `main` خود Alpine بود، نه در مخزن nginx.org. راه‌حل فقط یک خط بود — `--repository=https://.../alpine/latest-stable/main` که فقط به همان یک نصب محدود می‌شد — اما پیدا کردنش نیازمند این بود که بفهمیم رشته‌ی «نسخه‌ی رفع‌شده»ی یک CVE دقیقاً *کدام* مخزن آن را ارائه می‌دهد، و یک `apk upgrade` که به‌ظاهر منطقی است می‌تواند بی‌سروصدا هیچ چیز مهمی را آپدیت نکند.

حذف کردن بسته‌ها هم دستاورد بزرگ‌تری نسبت به وصله کردن آن‌ها بود: `curl` و `gnupg` در ایمیج بک‌اند «برای احتیاط» نصب شده بودند، بدون اینکه هیچ‌جا استفاده شوند. آن‌ها حدود ۴۰ مورد از یک اسکن ۱۰۲ موردی را تشکیل می‌دادند، که چند مورد از آن‌ها هنوز هیچ وصله‌ای برای نسخه‌ی نصب‌شده منتشر نشده بود. تنها راه‌حل واقعی برای یک آسیب‌پذیری در بسته‌ای که استفاده نمی‌کنید، این است که آن بسته را اصلاً ارسال نکنید.

## Rotsy امروز واقعاً چه کاری انجام می‌دهد

- **اسکن آسیب‌پذیری** — Trivy و Grype روی هر ایمیج اجرا می‌شوند، قابل مرور به‌صورت مخزن → ایمیج → تگ → گزارش، همراه با جزئیات هر CVE (نسخه‌ی نصب‌شده/رفع‌شده، CVSS) و دکمه‌ی **خروجی PDF** برای اشتراک‌گذاری یک گزارش خارج از ابزار.
- **رویدادمحور، نه polling** — اسکن‌ها با push (وب‌هوک یا watcher جایگزین) یا به‌صورت دستی اجرا می‌شوند، هرگز طبق یک زمان‌بندی کور.
- **RBAC با قوانین دسترسی به‌ازای هر مخزن** — خواندن/نوشتن/حذف که تا سطح مخازن و ایمیج‌های تکی محدود می‌شود، نه فقط همه‌یا‌هیچِ سطح نقش.
- **پشتیبان‌گیری زمان‌بندی‌شده و فشرده** — تناوب روزانه/هفتگی/ماهانه/cron، سیاست نگهداری قابل‌تنظیم، آرشیوهای `.tar.gz` به‌جای کپی مستقیم دایرکتوری.
- **پیشرفت job به‌صورت لحظه‌ای** — پیشرفت استریم‌شده با SSE برای اسکن‌ها، دانلود دیتابیس‌ها و پشتیبان‌گیری‌ها، همراه با لغو واقعی (نگاه کنید به بالا) به‌جای یک spinner که به شما دروغ می‌گوید.
- **بخش مستندات ساخته‌شده داخل خودِ برنامه** — راهنمای نصب، مرجع پیکربندی، عیب‌یابی، یادداشت‌های ارتقا، همه هم‌نسخه با کد.

![اسکرین‌شات: درخت آسیب‌پذیری مخزن → ایمیج → تگ](./screenshots/scan-tree-placeholder.png)

![اسکرین‌شات: خروجی PDF یک گزارش آسیب‌پذیری](./screenshots/pdf-export-placeholder.png)

![اسکرین‌شات: پیکربندی پشتیبان‌گیری زمان‌بندی‌شده](./screenshots/backup-schedule-placeholder.png)

*(اسکرین‌شات‌های بالا جایگزین موقت هستند — تصاویر واقعی را در یک دایرکتوری `screenshots/` قرار دهید و مسیرها را به‌روزرسانی کنید.)*

## پشته‌ی فناوری (Stack)

FastAPI + SQLAlchemy (async) + Postgres در بک‌اند، یک صف job مبتنی بر Redis (بدون Celery — چند صد خط کد برای رسیدن به pending → running → done/failed/cancelled همراه با پیشرفت SSE کافی بود)، React 19 + Vite در فرانت‌اند، Trivy و Grype برای اسکن. Python 3.13، Node 24 LTS.

## امتحانش کنید

```bash
git clone https://github.com/Sajed-Alavi/Rotsy.git
cd rotsy
cp .env.example .env   # مقداردهی JWT_SECRET، اطلاعات ادمین اولیه، اطلاعات Postgres
docker compose up --build
```

آن را به یک نمونه از Nexus Repository Manager با حداقل یک مخزن Docker وصل کنید، و مخازن به‌صورت خودکار شناسایی می‌شوند — بدون نیاز به هیچ پیکربندی جداگانه برای هر مخزن جهت شروع کار.

**مخزن کد:** [github.com/Sajed-Alavi/Rotsy](https://github.com/Sajed-Alavi/Rotsy)
**مجوز (License):** یک مجوز سفارشی با الزام حفظ اعتبار نویسنده — به فایل [`LICENSE`](./LICENSE) نگاه کنید. استفاده، تغییر و توزیع مجاز است؛ اما اعلامیه‌ی کپی‌رایت اصلی باید حفظ شود و نباید حذف، تغییر یا مبهم شود.
